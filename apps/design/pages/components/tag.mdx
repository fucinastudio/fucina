---
title: Tag
---

import { Tabs, Cards, Card } from "nextra/components";
import {
  TagPreview,
  TagBrand,
  TagNeutral,
  TagRed,
  TagAmber,
  TagEmerald,
  TagTeal,
  TagCyan,
  TagBlue,
  TagPurple,
  TagPink,
} from "@/components/preview/tag.tsx";

# 💠 Tag

Displays an element for categorizing or markup.

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagPreview />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag>
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

## Usage

The Tag component is a small label used to categorize or mark items with key information that enables quick recognition and navigation.

Tags can encapsulate short pieces of text, such as keywords, statuses, or descriptors, and are often interactive, allowing users to add or remove them as filters.

### When to use

- **Categorization**: Use Tags to assign categories or types to items, helping users to filter or identify content based on shared characteristics.
- **Metadata**: Implement Tags to display metadata, such as attributes of a product or article, which are not part of the main content but provide essential context.
- **Interactivity**: When Tags represent interactive elements, such as filters or selections in a search, use them to allow users to manipulate the displayed content.
- **Status indication**: Tags are effective for showing the status of items, like 'Completed', 'Pending', or 'New', providing a quick visual reference.

Avoid using Tags for long or complex information that would be better served by a different UI element like a Tooltip or a dedicated text field. Tags should be concise to improve legibility and usability.

## Examples

### Brand

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagBrand />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="brand"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="brand">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```
  </Tabs.Tab>
</Tabs>

### Neutral

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagNeutral />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="neutral"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="neutral">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```
  </Tabs.Tab>
</Tabs>

### Red

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagRed />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="red"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="red">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

### Amber

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagAmber />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="amber"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="amber">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

### Emerald

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagEmerald />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="emerald"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="emerald">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

### Teal

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagTeal />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="teal"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="teal">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

### Cyan

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagCyan />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="cyan"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="cyan">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

### Blue

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagBlue />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="blue"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="blue">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

### Purple

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagPurple />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="purple"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="purple">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

### Pink

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <TagPink />
  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy {6} /variant="pink"/ filename="tag.tsx"
    import { Tag } from "@feely/ui/components/tag";
    import { TagIcon } from 'lucide-react';

    export function TagDemo() {
        return (
            <Tag variant="pink">
                <TagIcon /> Markup
            </Tag>
        );
    }
    ```

  </Tabs.Tab>
</Tabs>

## Useful links

**Here you go!** Here are all the resources you might need if you are a designer, a developer or a content manager:

<Cards className="mt-8">
  <Card title="🤖 Storybook" href="https://www.stories.feely.so/?path=/docs/components-tag--documentation" />
  <Card
    title="🪄 Figma"
    href="https://www.figma.com/design/Jfto7FUoU7mSpnv9uESD60/%F0%9F%9F%A0-feely---design-system?node-id=635-3323&t=nua4UmG9Iu1hfUAm-1"
  />
</Cards>
